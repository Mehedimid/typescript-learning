{
    // ======================= starts =====================

    function reverseArray <T> (arr : T[]) : T[]{
        return arr.reverse()
    }
    const result = reverseArray(["rahim", "karim", "mona "])
    console.log(result);
// ======================= ends =====================
}