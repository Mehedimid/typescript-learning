"use strict";
{
    // ======================= starts =====================
    function reverseArray(arr) {
        return arr.reverse();
    }
    const result = reverseArray(["rahim", "karim", "mona "]);
    console.log(result);
    // ======================= ends =====================
}
